[project]
name = "streamlit-lab-snowflake"
version = "0.1.0"
description = "Isolated Snowflake lesson environment until its DataFrame extra supports Pandas 3"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pandas>=2.3,<3",
    "snowflake-connector-python[pandas]>=4.7.1,<5",
    "streamlit>=1.64,<2",
]

[tool.uv]
package = false
